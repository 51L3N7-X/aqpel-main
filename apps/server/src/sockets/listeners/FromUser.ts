import type { Socket } from "socket.io-client";

// from user is waiter or bill or embers

export const fromUser = async (order: Object, socket: Socket) => {
  try {
    //TODO: check order using joi
    
  } catch (e) {
    
  }
};

